# Copy to apps/worker/.dev.vars for local system diagnostics.
# Generate a high-entropy value, for example:
# openssl rand -hex 32
#
# CHANGE_ME is intentionally too short and will be rejected by the Worker.
SYSTEM_DIAGNOSTICS_TOKEN=CHANGE_ME
