import {
  describe,
  expect,
  it
} from 'vitest'


describe(
  'Table Business API',
  () => {
    it(
      'loads the table API test suite',
      () => {
        expect(true)
          .toBe(true)
      }
    )
  }
)